# Review notes

The build{++ step++} runs first.

Drop the {--redundant--} flag.

Change {~~old value~>new value~~} before merge.

The {==important part==} needs another look.

Rename {--`oldFunc`--} across the API.

Literal markers like `{++not a suggestion++}` stay code.

**Bold** {++and an insertion++} sit side by side.

Emoji survive: {++☕ 😀++}.
